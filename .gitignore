# Credentials — never commit
credentials/

# Terraform
**/.terraform/
*.tfstate*
.terraform.lock.hcl
api/mysql-proxy
api/autoscale-checker
api/metrics-collector

# Talos cluster configs (secrets)
infra/talos/kubeconfig
infra/talos/talosconfig
