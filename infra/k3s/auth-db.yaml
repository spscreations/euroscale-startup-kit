apiVersion: apps/v1
kind: Deployment
metadata:
  name: euroscale-auth-db
  namespace: euroscale
spec:
  replicas: 1
  selector:
    matchLabels:
      app: euroscale-auth-db
  template:
    metadata:
      labels:
        app: euroscale-auth-db
    spec:
      containers:
      - name: mariadb
        image: mariadb:11.4
        env:
        # Password lives in euroscale-auth-db-credentials (see infra/secrets/auth-db-secret.yaml).
        # Only applied on first MariaDB init; changing the secret later requires a password rotation.
        - name: MARIADB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: euroscale-auth-db-credentials
              key: password
        - name: MARIADB_DATABASE
          value: euroscale_auth
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
      volumes:
      - name: data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: euroscale-auth-db
  namespace: euroscale
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: euroscale-auth-db
